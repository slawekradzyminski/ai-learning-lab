export const LLM_COURSE_PROMPT = 'The animal did not cross the street because it was too';
export const LLM_COURSE_TARGET = ' tired';
export const LLM_COURSE_SENTENCE = `${LLM_COURSE_PROMPT}${LLM_COURSE_TARGET}.`;

export const LLM_COURSE_ATTENTION_WINDOW = ['animal', 'street', 'was', 'too'];
